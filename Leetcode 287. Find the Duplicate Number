class Solution {
public:
    int findDuplicate(vector<int>& nums) {
        unordered_set<int> st;
        for(int i : nums){
            if(!st.count(i)){
                st.insert(i);
            }
            else{
                return i;
            }
        }
        return -1;

        // int slow = nums[0];
        // slow = nums[slow];
        // int fast = nums[0];
        // fast = nums[nums[fast]];
        // while (slow != fast) {                                     
        //     slow = nums[slow];
        //     fast = nums[nums[fast]];
        // }
        // slow = nums[0];

        // while (slow != fast) {
        //     slow = nums[slow];
        //     fast = nums[fast];
        // }
        // return fast;
    }
};
